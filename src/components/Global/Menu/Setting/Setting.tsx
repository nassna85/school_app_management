import Divider from "@/components/Global/UI/Divider/Divider";

const Setting = () => {
  return (
    <>
      <small className="text-gray-400 uppercase text-xs">Settings</small>
      <Divider />
    </>
  );
};

export default Setting;
